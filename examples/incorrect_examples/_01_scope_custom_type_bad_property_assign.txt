// Scope tests: property access must match the custom type fields exactly
// EXPECTED: INVALID (one error) -> property 'age' not in Person

TYPE Person
    DECLARE name : STRING
ENDTYPE

DECLARE p : Person

// ERROR: 'age' is not a declared field of Person
p.age <- 10
