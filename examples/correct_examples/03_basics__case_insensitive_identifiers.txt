// TEST: identifiers are case-insensitive
// EXPECTED: valid
// BEHAVIOR_CAPTURED_BY_OUTPUT: identifier normalization across cases (MyVar/myvar/MYVAR) and procedure call with mixed-case name

DECLARE MyVar : INTEGER

MyVar <- 10
myvar <- myvar + 5
OUTPUT "myvar = ", MYVAR

PROCEDURE ShowValue(v : INTEGER)
    OUTPUT "v = ", v
ENDPROCEDURE

CALL showvalue(MyVar)
