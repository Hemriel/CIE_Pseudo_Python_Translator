// TEST: numeric built-ins (INT, RAND)
// EXPECTED: valid
// BEHAVIOR_CAPTURED_BY_OUTPUT: INT(real) conversion to INTEGER and RAND(seed) producing a REAL (validated via regex in harness)

DECLARE x : REAL
DECLARE i : INTEGER
DECLARE r : REAL

x <- 27.5415
i <- INT(x)
OUTPUT "INT(", x, ") = ", i

r <- RAND(87)
OUTPUT "RAND(87) = ", r