// TEST: scope (function params shadow globals)
// EXPECTED: valid
// BEHAVIOR_CAPTURED_BY_OUTPUT: shadowing behavior via function parameter vs global value, with both printed to demonstrate scope resolution

DECLARE x : INTEGER
x <- 5

FUNCTION UseX(x : INTEGER) RETURNS INTEGER
    // 'x' here should refer to the parameter, not the global
    RETURN x + 1
ENDFUNCTION

DECLARE y : INTEGER
y <- UseX(100)

OUTPUT "y=", y
OUTPUT "global x=", x
