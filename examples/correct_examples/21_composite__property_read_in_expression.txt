// TEST: property read used inside expressions
// EXPECTED: valid
// BEHAVIOR_CAPTURED_BY_OUTPUT: property read (p.Age) contributing to computed result (nextAge) and printed outcome

TYPE Person
    DECLARE Age : INTEGER
ENDTYPE

DECLARE p : Person
p.Age <- 40

DECLARE nextAge : INTEGER
nextAge <- p.Age + 1

OUTPUT "nextAge=", nextAge
