// Scope tests: FOR does NOT create a local scope; loop variable remains visible after NEXT
// EXPECTED: valid

DECLARE sum : INTEGER
sum <- 0

DECLARE i : INTEGER

FOR i <- 0 TO 3
    sum <- sum + i
NEXT i

OUTPUT "sum=", sum
// Since FOR does not create a scope, 'i' is still visible here
OUTPUT "i=", i
