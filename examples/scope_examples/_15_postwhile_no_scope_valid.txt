// Scope tests: REPEAT/UNTIL (post-while) does NOT create a new local scope
// EXPECTED: valid

DECLARE k : INTEGER
k <- 0

REPEAT
    DECLARE msg : STRING
    msg <- "loop"
    k <- k + 1
UNTIL k = 1

// Since REPEAT/UNTIL does not create a scope, 'msg' is still visible here
OUTPUT msg
