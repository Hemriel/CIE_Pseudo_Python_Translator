// Scope tests: declarations inside IF remain visible after ENDIF
// EXPECTED: valid

IF TRUE THEN
    DECLARE t : INTEGER
    t <- 99
    OUTPUT "inside IF t=", t
ENDIF

// Since IF does not create a scope, 't' is still visible here
OUTPUT "after IF t=", t
